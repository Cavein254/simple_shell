#include "c_shell.h"

size_t no_tkns(*str, char delimiter) {
  size_t tkns;

  tkns = 0;
}

char **no_strtok(*str, char delimiter) {
  size_t tkns;

  tkns = 0;
  tkns = no_tkns(str, delimiter);
}

int main() { return (0); }
